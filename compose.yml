services:
  postgres:
    image: postgres:17-alpine
    container_name: gitddo-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-gitddo}
      POSTGRES_USER: ${POSTGRES_USER:-gitddo}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-gitddo}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-gitddo} -d ${POSTGRES_DB:-gitddo}"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
